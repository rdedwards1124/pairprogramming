
const synonyms = ['fantastic', 'wonderful', 'great']
const greetings = []

/*----- For Loop Steps -----*/
// 1.Intialing counter variable, 
// 2.Setting conditional, 
// 3.Running instructional, 
// 4.Increment the counter.

// Template literal: ${ }

for(let i = 0; i < synonyms.length; i++){
    // Push each syn into greeting
}

/////////////////////////////////////////////////////

const x = 3

function isX1 (num){
    const x = 5
    return num === x
}

function isX2 (num){
    return num === x
}

const one = isX1(5)
const two = isX2(3)

// Declaring a constant variable and setting it equal to the Evaluated result of isX1 with an argument of 5.